---

##### GLOBAL METADATA

- meta:
    cluster: apm-ci

##### JOB DEFAULTS

- job:
    view: APM-CI
    logrotate:
      numToKeep: 300
    node: linux
    concurrent: true
    publishers:
    - email:
        recipients: infra-root+build@elastic.co
